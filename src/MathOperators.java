public class MathOperators {
    public void calc() {
        int tuna = 5;
        int bass = 18;

        //Increment variable allows you to add 1
        //++tuna;
        //pre-incrementing: Changes the value of the variable before it uses it
        System.out.println(++tuna);
        //post-incrementing (System.out.println(tuna++);)
        /*
        Math Operators:
        * - Multiplication
        / - Division
        + - Addition
        - - Subtraction
        = - Assign an operator
         */
    }
}
